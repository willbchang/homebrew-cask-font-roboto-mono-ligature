# Homebrew Roboto Mono without SVN
A brew font cask that uses **Git** to install **Roboto Mono** font instead of SVN.

## Usage
```bash
brew tap willbchang/homebrew-cask-font-roboto-mono
brew install --cask font-roboto-mono-no-svn
```
